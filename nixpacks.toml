[phases.setup]
nixPkgs = ["gcc", "gfortran", "python311Packages.openblas"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install numpy==1.24.3",
  "pip install pandas==1.5.3",
  "pip install -r requirements.txt"
]
